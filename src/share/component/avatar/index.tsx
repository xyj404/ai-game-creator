import { Input } from 'antd';
import React from 'react';

import './index.less';

export const Dialog = () => {
  return (
    <div className={'dialog-wrapper'}>
      <Input></Input>
    </div>
  );
};
